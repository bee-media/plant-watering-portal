# 🚀 Быстрый старт - Система управления поливом растений

## Минимальные шаги для запуска

### 1. Установка зависимостей (5 минут)

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте его
# Windows:
venv\Scripts\activate
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка MySQL (5 минут)

```bash
# Создайте базу данных и импортируйте схему
mysql -u root -p < database.sql

# Или выполните инициализацию через Python
python init_db.py
```

### 3. Конфигурация (2 минуты)

```bash
# Скопируйте файл примера
cp .env.example .env

# Отредактируйте .env и укажите:
# - DB_PASSWORD (пароль MySQL)
# - SECRET_KEY (сгенерируйте: python -c "import secrets; print(secrets.token_hex(32))")
# - TELEGRAM_BOT_TOKEN (получите у @BotFather, см. инструкцию ниже)
```

### 4. Создание Telegram бота (3 минуты)

1. Откройте Telegram
2. Найдите @BotFather
3. Отправьте `/newbot`
4. Укажите имя: "My Plant Bot"
5. Укажите username: "my_plant_watering_bot"
6. Скопируйте токен в .env файл

### 5. Создание пользователя (2 минуты)

```bash
# Запустите скрипт управления пользователями
python manage_users.py

# Выберите: 1. Создать нового пользователя
# Введите:
# - Имя: Ваше имя
# - Логин: ваш_логин
# - Пароль: ваш_пароль (минимум 6 символов)
# - Telegram ID: (получите от бота, пока можно пропустить)
# - Уведомления: y
```

### 6. Запуск (1 минута)

```bash
# Запустите приложение
python app.py

# Откройте браузер
# http://localhost:5000

# Войдите с созданными данными
```

### 7. Настройка уведомлений (2 минуты)

1. Найдите своего бота в Telegram
2. Отправьте `/start`
3. Скопируйте ваш Telegram ID
4. В портале: Пользователи → Редактировать → вставьте ID
5. Включите "Получать уведомления"

## 🎉 Готово!

Теперь вы можете:
- Добавлять растения
- Получать уведомления в Telegram
- Отмечать полив одной кнопкой
- Отслеживать историю

## 📚 Полная документация

См. файл README.md для детальной информации.

## 💡 Подсказки

- Примеры растений: `mysql -u root -p plant_watering < sample_plants.sql`
- Управление пользователями: `python manage_users.py`
- Telegram ID: отправьте `/start` вашему боту
- Время уведомлений: настраивается в разделе "Настройки"

---

**Время установки: ~20 минут** ⏱️

## 🛠️ Управление пользователями

Скрипт `manage_users.py` позволяет:
- ✅ Создавать новых пользователей
- 📋 Просматривать список всех пользователей
- 🗑️ Удалять пользователей
- 🔑 Сбрасывать пароли

Просто запустите: `python manage_users.py`
