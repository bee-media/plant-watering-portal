# 🌱 Система управления поливом растений

Веб-портал для управления поливом домашних растений с уведомлениями в Telegram.

## 🎨 Особенности

- ✅ **Красивый интерфейс** в стиле растений с зеленой палитрой
- 🔐 **Авторизация пользователей** с управлением доступом
- 🌿 **Управление растениями** - добавление, редактирование, удаление
- 📅 **Автоматическое расписание** полива и прикормки
- 📱 **Telegram уведомления** с интерактивными кнопками
- 📊 **Дашборд** с актуальной статистикой
- 📝 **История действий** по каждому растению
- ⏰ **Гибкие настройки** времени и интервалов уведомлений
- 🔄 **Автоматические повторы** уведомлений

## 🛠 Технологии

- **Backend**: Python 3.8+, Flask
- **База данных**: MySQL 8.0+
- **Frontend**: HTML5, CSS3, JavaScript
- **Уведомления**: python-telegram-bot
- **Планировщик**: APScheduler

## 📋 Требования

- Python 3.8 или выше
- MySQL 8.0 или выше
- Telegram бот (токен от @BotFather)

## 🚀 Установка

### 1. Клонирование и настройка окружения

```bash
# Создайте виртуальное окружение
python -m venv venv

# Активируйте виртуальное окружение
# Для Windows:
venv\Scripts\activate
# Для Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt
```

### 2. Настройка базы данных MySQL

```bash
# Войдите в MySQL
mysql -u root -p

# Создайте базу данных и импортируйте схему
mysql -u root -p < database.sql
```

Или выполните команды из `database.sql` вручную в MySQL.

### 3. Создание Telegram бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям:
   - Введите имя бота (например: "Plant Watering Bot")
   - Введите username бота (например: "my_plant_bot")
4. Скопируйте полученный токен (формат: `1234567890:ABCdefGHIjklMNOpqrsTUVwxyz`)

### 4. Настройка конфигурации

```bash
# Скопируйте файл примера
cp .env.example .env

# Отредактируйте .env файл
nano .env
```

Заполните следующие параметры:

```env
# База данных
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=your_mysql_password
DB_NAME=plant_watering

# Flask
SECRET_KEY=your_secret_key_here  # Сгенерируйте: python -c "import secrets; print(secrets.token_hex(32))"

# Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here

# Часовой пояс
TIMEZONE=Europe/Moscow
```

### 5. Генерация секретного ключа

```bash
python -c "import secrets; print(secrets.token_hex(32))"
```

Скопируйте результат в параметр `SECRET_KEY` в файле `.env`.

## 🎯 Запуск приложения

### Создание первого пользователя

После инициализации базы данных создайте первого пользователя:

```bash
python manage_users.py
```

Скрипт предоставляет интерактивное меню для:
- Создания новых пользователей
- Просмотра списка пользователей
- Удаления пользователей
- Сброса паролей

### Обычный запуск

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

### Запуск в production режиме (рекомендуется с Gunicorn)

```bash
# Установите Gunicorn
pip install gunicorn

# Запустите приложение
gunicorn -w 4 -b 0.0.0.0:5000 app:app
```

## 👤 Создание первого пользователя

После установки и настройки базы данных создайте первого пользователя:

```bash
python manage_users.py
```

Выберите опцию "1. Создать нового пользователя" и следуйте инструкциям.

**Рекомендуемые данные:**
- Имя: Ваше имя
- Логин: ваш_логин (латиницей, без пробелов)
- Пароль: надежный пароль (минимум 6 символов)
- Telegram ID: получите, отправив /start вашему боту
- Уведомления: да (y)

## 📱 Настройка Telegram уведомлений

### Для каждого пользователя:

1. Войдите в систему
2. Найдите своего Telegram бота (используйте username, который вы создали)
3. Отправьте боту команду `/start`
4. Скопируйте полученный Telegram ID
5. Зайдите в раздел "Пользователи" → Редактировать свой профиль
6. Вставьте Telegram ID в соответствующее поле
7. Убедитесь, что галочка "Получать уведомления" установлена
8. Сохраните изменения

### Настройка параметров уведомлений:

1. Перейдите в раздел "Настройки"
2. Установите временной диапазон для уведомлений (по умолчанию 8:00 - 22:00)
3. Настройте интервал повтора уведомлений (по умолчанию 120 минут)
4. Укажите максимальное количество повторов (по умолчанию 3)
5. Сохраните настройки

## 📚 Использование системы

### Добавление растения

1. Перейдите в раздел "Растения"
2. Нажмите "Добавить растение"
3. Заполните форму:
   - Название растения
   - Интервал полива (дни)
   - Интервал прикормки (опционально)
   - Местоположение (опционально)
   - Описание (опционально)
   - Фотография (опционально)
4. Нажмите "Добавить растение"

### Работа с уведомлениями

Когда приходит время полить растение:
1. Вы получите уведомление в Telegram с кнопкой "✅ Я полью"
2. Нажмите на кнопку после полива
3. Другие пользователи получат уведомление о том, что вы полили растение
4. Система автоматически запланирует следующий полив

Если никто не нажал кнопку:
- Уведомление повторится через указанный интервал
- Повторы продолжатся до максимального количества раз

### Команды Telegram бота

После настройки бота вам доступны следующие команды:

- `/start` - Получить ваш Telegram ID для регистрации
- `/plants` - Показать список всех растений с кнопками для просмотра деталей
- `/status` - Показать текущий статус всех растений:
  - Какие требуют полива (с указанием просрочки)
  - Какие требуют прикормки
  - Какие растения в порядке (дни до следующего полива)
- `/help` - Справка по всем командам

**Детальная информация о растении:**
- Нажмите кнопку "📊 Подробнее" под любым растением
- Увидите:
  - Описание и местоположение
  - Интервалы полива и прикормки
  - Дни до следующего ухода (или просрочку)
  - Последние 3 действия с растением (кто и когда поливал)

**Пример использования:**
```
/status
📊 Статус растений

💧 Требуют полива:
  🔴 Фикус (просрочено 2 дн.)
  ⚠️ Алоэ (сегодня)

✅ В порядке:
  🟢 Монстера (полив через 3 дн.)
  🟢 Кактус (полив через 15 дн.)
```

## 🔧 Структура проекта

```
plant_watering_portal/
├── app.py                  # Главное приложение Flask
├── config.py              # Конфигурация
├── database.py            # Модели данных и работа с БД
├── telegram_bot.py        # Telegram бот
├── scheduler.py           # Планировщик задач
├── manage_users.py        # Управление пользователями
├── init_db.py            # Инициализация БД
├── run_bot.py            # Запуск бота отдельно
├── database.sql           # SQL схема базы данных
├── sample_plants.sql      # Примеры растений
├── requirements.txt       # Зависимости Python
├── .env.example          # Пример конфигурации
├── templates/            # HTML шаблоны
│   ├── base.html
│   ├── login.html
│   ├── dashboard.html
│   ├── plants.html
│   ├── plant_form.html
│   ├── plant_history.html
│   ├── users.html
│   ├── user_form.html
│   └── settings.html
├── static/               # Статические файлы
│   ├── css/
│   │   └── style.css
│   ├── js/
│   │   └── main.js
│   └── uploads/         # Загруженные изображения
├── README.md            # Документация
└── QUICKSTART.md        # Быстрый старт
```

## 🎨 Дизайн

Система оформлена в стиле растений с использованием:
- Зеленая палитра (#2d6a4f, #52b788, #74c69d)
- Шрифты: Playfair Display (заголовки), Quicksand (основной текст)
- Декоративные элементы в виде листьев
- Плавные анимации и переходы
- Адаптивный дизайн для всех устройств

## 🔒 Безопасность

- Пароли хешируются с использованием bcrypt
- Защита от SQL-инъекций через параметризованные запросы
- Сессионная аутентификация с Flask-Login
- Валидация всех пользовательских данных
- Ограничение размера загружаемых файлов (16 МБ)

## ⚙️ Настройка планировщика

Система автоматически:
- Проверяет необходимость отправки уведомлений каждый час
- Проверяет повторные уведомления каждые 30 минут
- Работает только в указанные часы (настраивается в интерфейсе)
- Использует московское время (UTC+3)

## 🐛 Решение проблем

### Ошибка подключения к базе данных

Убедитесь, что:
- MySQL сервер запущен
- Данные для подключения в `.env` корректны
- База данных создана и таблицы импортированы

### Уведомления не приходят

Проверьте:
- Токен бота в настройках системы корректен
- У пользователей указан Telegram ID
- Включены уведомления в профиле пользователя
- Текущее время попадает в диапазон отправки

### Бот не отвечает на команды

- Перезапустите приложение
- Проверьте токен бота
- Убедитесь, что бот не заблокирован в Telegram

## 📝 Логирование

Логи приложения выводятся в консоль. Для production рекомендуется настроить запись в файл:

```python
import logging

logging.basicConfig(
    level=logging.INFO,
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    handlers=[
        logging.FileHandler('plant_watering.log'),
        logging.StreamHandler()
    ]
)
```

## 🚀 Развертывание в production

### Использование systemd (Linux)

Создайте файл `/etc/systemd/system/plant-watering.service`:

```ini
[Unit]
Description=Plant Watering Portal
After=network.target mysql.service

[Service]
User=www-data
Group=www-data
WorkingDirectory=/path/to/plant_watering_portal
Environment="PATH=/path/to/plant_watering_portal/venv/bin"
ExecStart=/path/to/plant_watering_portal/venv/bin/gunicorn -w 4 -b 0.0.0.0:5000 app:app

[Install]
WantedBy=multi-user.target
```

Запустите сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable plant-watering
sudo systemctl start plant-watering
```

### Nginx конфигурация

```nginx
server {
    listen 80;
    server_name your-domain.com;

    location / {
        proxy_pass http://127.0.0.1:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    location /static {
        alias /path/to/plant_watering_portal/static;
    }
}
```

## 🤝 Поддержка

Если у вас возникли вопросы или проблемы:
1. Проверьте документацию выше
2. Проверьте логи приложения
3. Убедитесь, что все зависимости установлены

## 📄 Лицензия

Этот проект создан для личного использования. Вы можете свободно использовать, модифицировать и распространять его.

## 🌟 Особенности реализации

- **Архитектура**: MVC паттерн с разделением логики
- **База данных**: Оптимизированные индексы для быстрых запросов
- **Планировщик**: APScheduler с поддержкой часовых поясов
- **Telegram**: Асинхронная обработка сообщений
- **Frontend**: Современный адаптивный дизайн без фреймворков
- **Безопасность**: Хеширование паролей, защита от CSRF

---

**Приятного использования! 🌱**
